"""Concrete implementations of the core service contracts (CV1.E7).

Domain code never imports these directly — it depends on the protocols in
:mod:`api.contracts` and receives an implementation from :mod:`api.infra.providers`.
"""
