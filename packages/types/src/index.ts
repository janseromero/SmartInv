/**
 * SmartInv shared TypeScript types and Zod schemas.
 *
 * Single source of truth for domain types consumed by the web app,
 * future native app, and (via codegen) the FastAPI service.
 *
 * Real types and schemas are added as domain tasks land.
 */

export const TYPES_READY = false as const;
