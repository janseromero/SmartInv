/**
 * SmartInv design tokens — source of truth for colors, spacing,
 * typography, radii, and shadows.
 *
 * Real values are populated in task E1.2 (sourced from the HTML mock).
 * This placeholder export keeps TypeScript and consumers happy until then.
 */

export const TOKENS_READY = false as const;
