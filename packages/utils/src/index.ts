/**
 * SmartInv shared utilities.
 *
 * Small framework-agnostic helpers consumed across apps and packages.
 * Real utilities are added as the codebase grows; nothing belongs here
 * by default — domain logic lives in services and feature modules.
 */

export const UTILS_READY = false as const;
